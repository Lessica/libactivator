# AGENTS.md

本文件定义本仓库中 AI 协作者的基础工作方式。除非用户明确给出更新指令，后续协作应持续遵守这些规则。

## 交流语言

- 与用户交流时使用简体中文。
- 解释思路、汇报进度、总结结果、询问问题时，都使用简体中文。
- 引用外部 API、系统符号、文件路径、命令、错误码、类名、方法名、常量名时，保留其原文。

## 代码语言

- 编写或修改代码时，代码内容必须使用英语。
- 代码注释必须使用英语。
- 运行时日志、调试日志、错误日志、诊断消息必须使用英语。
- 标识符、文件名、目录名、构建脚本、测试名、提交信息默认使用英语。
- 本地化资源除外；面向用户显示的可本地化文本可以按资源文件的语言要求编写。

## 协作方式

- 先理解现有规约、Public API 和参考实现，再进行实现决策。
- 每次新会话或重新接手任务时，先读 `docs/README.md`，再按任务触碰范围读取 `docs/PROJECT_CONVENTIONS.md`、`docs/DEVICE_WORKFLOW.md`、`docs/TESTING.md`、`docs/LEGACY_REVERSE_ENGINEERING.md`。
- 涉及内置 event / listener / action 路线或逐项状态时，再读取 `docs/BUILT_IN_ROADMAP.md` 和 `docs/BUILT_IN_ACTION_TRACKER.md`。
- `docs/archive/` 下的文档是历史归档，只用于追溯先前决策、证据和旧 inventory；如果根目录新文档与归档文档冲突，以根目录新文档为准。
- 保持改动范围清晰，避免把无关重构混入当前任务。
- 不要提交 `references/`；它只是本地参考快照。
- 新增重要工程约定时，优先更新 `docs/PROJECT_CONVENTIONS.md`；涉及真机交互、测试分类或内置能力路线时，分别更新对应的新文档。
- `docs/` 目录下的文档不要按固定行宽硬换行；普通段落应保持一段一行。
- `docs/` 根目录下的新文档使用简体中文编写，能用中文描述的尽量用中文。
- 如果后续重新新增语言副本目录，例如 `docs/zh-Hans`，其中的文档也必须使用简体中文编写。
- 遇到不确定的兼容性、越狱布局、IPC、SpringBoard 私有 API 选择时，先记录待决策点，再与用户确认。

## 验证方式

- 完成代码改动后，应尽量运行与改动范围匹配的格式化、构建或测试命令。
- 如果无法验证，应在最终回复中说明原因。
- 验证输出中新增的日志或诊断文本也必须使用英语。
